export class Tag {
  constructor(
    public id: number,
    public name: string,
    public contentId: number,
    public url: string,
    public description: string,
    public category: string
  ) {
  }
}
